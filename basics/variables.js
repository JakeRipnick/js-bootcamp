// 1. You can't define a variable more than once
let petName = 'Diesel';
petName = 'Chica';

console.log(petName);

// 2. There are rules related to Variable names - letters, _, and $, are valid

// 3. Variable names cannot be reserved key words
